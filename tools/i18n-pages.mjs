export const PAGES = {
  en: {
    showcaseTitle: 'NotionKit Showcase – every component, both themes',
    showcaseDesc: 'A systematic single view of every NotionKit component, in light and dark mode.',
    showcaseH1: 'Showcase',
    showcaseLead: 'Every component in the library, one after another, with nothing else on the page. Flip the theme in the top right to check both palettes.',
    docsTitle: 'NotionKit Docs – components, tokens and integration',
    docsDesc: 'The full NotionKit reference: live previews, copy-paste markup, design tokens for both themes, state classes, mobile behaviour and editor integration recipes.',
    docsH1: 'Documentation',
    docsLead: 'Every component with a live preview, the markup to copy, the classes involved and what it does on a small screen. Nothing here is generated from a screenshot — the previews are the real thing.',
    onThisPage: 'On this page',
    sections: 'Sections',
    components: 'Components',
    secInstall: 'Installation', secTokens: 'Design tokens', secTheming: 'Theming',
    secStates: 'State classes', secContrast: 'Contrast', secEditor: 'Editor integration',
    secShadow: 'Web components', secGroups: 'Component reference',
    installLead: 'One stylesheet. Pick whichever of these fits your project.',
    installTheme: 'The theme lives on <code class="nk-inline-code">&lt;html&gt;</code>. Light is the default, so the attribute is only needed for dark:',
    installBody: 'Add <code class="nk-inline-code">class="nk-body"</code> to your <code class="nk-inline-code">&lt;body&gt;</code> to pick up the font, background and text colour. It is opt-in: without it, NotionKit touches nothing outside its own classes.',
    tokensLead: 'Every visual value is a custom property. The two blocks below are the entire token surface — <code class="nk-inline-code">:root</code> for light, <code class="nk-inline-code">[data-theme="dark"]</code> for the overrides.',
    tokenRebrand: 'Re-branding is one declaration. Everything derived — the focus ring, the checked states, the selected model card, the danger hover — is mixed from a token with <code class="nk-inline-code">color-mix()</code>, so it follows along:',
    colToken: 'Token', colLight: 'Light', colDark: 'Dark', colMeaning: 'What it is',
    themingLead: 'Switch by setting one attribute. Nothing else moves.',
    themingNote: 'The token blocks also set <code class="nk-inline-code">color-scheme</code>, so browser-drawn UI — date pickers, number spinners, select popups, scrollbars, autofill — follows the theme without any extra work.',
    themingPalettes: 'To see re-branding in action, use the 🎨 button in the top bar: it applies the example palettes from <code class="nk-inline-code">theme-override.css</code> (Forest, Slate, Sunset, High contrast) live on this page. The demo app has the same switch under Settings → Appearance.',
    statesLead: 'NotionKit ships states, not behaviour. Add and remove these classes yourself; there is no JavaScript in the library.',
    colClass: 'Class', colAppliesTo: 'Applies to', colEffect: 'Effect',
    contrastLead: 'Measured contrast ratios for the default palette against the surface each pair actually sits on. Body text clears WCAG AA comfortably in both themes. Secondary and tertiary text, the tag pairs, the primary button label and the danger colour sit below 4.5:1 — these are Notion’s own values, kept deliberately.',
    contrastNote: 'If your project needs AA everywhere, <code class="nk-inline-code">theme-override.css</code> ships a ready high-contrast block: uncomment it and the failing pairs are lifted, with the rest of the design untouched.',
    colPair: 'Pair', colRatio: 'Ratio', colVerdict: 'AA body text',
    editorLead: 'NotionKit ships no editor. What it does ship is <code class="nk-inline-code">.nk-block-host</code> — the optical shell — plus a themed adapter layer scoped inside it. The integration contract is one class on the host element.',
    editorTip: 'TipTap / ProseMirror — the vanilla-JS path',
    editorTipBody: 'Load TipTap from a CDN as an ES module and mount it into an element carrying <code class="nk-inline-code">nk-block-host</code>. The adapter rules do the rest: typography, headings, lists, quotes, code, links, selection and the placeholder are all styled from <code class="nk-inline-code">--nk-*</code> tokens.',
    editorBn: 'BlockNote — the React path',
    editorBnBody: 'Same idea: render the BlockNote view inside a block host. <code class="nk-inline-code">.bn-container</code>, <code class="nk-inline-code">.bn-editor</code>, <code class="nk-inline-code">.bn-block-content</code>, the side menu and the drag handle are all covered, and <code class="nk-inline-code">.bn-suggestion-menu</code> picks up the slash-menu look automatically.',
    editorNovel: 'Novel — the Next.js path',
    editorNovelBody: 'Novel builds on ProseMirror, so the TipTap rules already apply. <code class="nk-inline-code">.novel-editor</code> and <code class="nk-inline-code">.novel-prose</code> get the font and colour, and the prose width cap is released so the page column governs it.',
    editorLive: 'Live: TipTap running inside a block host',
    editorLiveBody: 'The editor below is real TipTap, loaded from a CDN, with no CSS of its own. Hover a block for the ＋ / ⠿ rail and drag it, select text for the bubble toolbar, press <kbd class="nk-kbd">/</kbd> for the slash menu, and switch the theme — everything you see comes from the adapter section of notionkit.css.',
    editorRecipeNote: 'The full recipe — slash menu on <code class="nk-inline-code">@tiptap/suggestion</code>, bubble toolbar, block handle with drag &amp; drop and a block menu — is <a href="docs-editor.js">docs-editor.js</a>, about 300 lines of plain JavaScript. It puts NotionKit classes on its DOM (<code class="nk-inline-code">nk-slash-menu</code>, <code class="nk-inline-code">nk-bubble-menu</code>, <code class="nk-inline-code">nk-block-actions</code>, <code class="nk-inline-code">nk-menu</code>) and lets the stylesheet do the rest.',
    shadowLead: 'The stylesheet ships split in two so web components can adopt the component rules without re-declaring tokens inside their shadow roots.',
    shadowWhy: 'Why the split matters',
    shadowWhyBody: 'A shadow root that adopts the whole sheet also adopts the <code class="nk-inline-code">:root</code> and <code class="nk-inline-code">[data-theme]</code> blocks. Inside that root the selectors match the element’s own theme wrapper, so every token is re-declared locally — and a matching rule always beats an inherited value. A consumer’s own <code class="nk-inline-code">:root { --nk-accent: … }</code> would then never arrive. Adopt <code class="nk-inline-code">componentsSheet</code> only, and put <code class="nk-inline-code">tokensCss</code> on the document.',
    shadowSlotted: 'The ::slotted() twins',
    shadowSlottedBody: 'Content passed in from outside stays in the light DOM and is no descendant of anything in the shadow tree, so <code class="nk-inline-code">.nk-callout .c-icon</code> cannot reach it. Every such rule therefore has a <code class="nk-inline-code">::slotted()</code> twin beside it — 57 of them. Two limits: <code class="nk-inline-code">::slotted()</code> matches only the assigned node, never inside it (pass icons directly, never wrapped), and for slotted content the outer tree wins over the shadow tree.',
    shadowPoc: 'A working proof of concept ships as <a href="elements-poc.html">elements-poc.html</a> — an <code class="nk-inline-code">&lt;nk-callout&gt;</code> and an <code class="nk-inline-code">&lt;nk-tag color="…"&gt;</code> that adopt <code class="nk-inline-code">componentsSheet</code>, re-theme through <code class="nk-inline-code">data-theme</code> and take the page’s branding. The page measures all of that live (it needs a build and a server: <code class="nk-inline-code">npm run build</code>, then serve the folder).',
    mobileLabel: 'On a small screen',
    classesLabel: 'Classes',
    prdLabel: 'Group',
    backToTop: '↑ Top',
  },
  de: {
    showcaseTitle: 'NotionKit Showcase – jede Komponente, beide Themes',
    showcaseDesc: 'Eine systematische Einzelansicht jeder NotionKit-Komponente, in Hell und Dunkel.',
    showcaseH1: 'Showcase',
    showcaseLead: 'Jede Komponente der Bibliothek, eine nach der anderen, ohne Ablenkung. Oben rechts das Theme umschalten, um beide Paletten zu prüfen.',
    docsTitle: 'NotionKit Doku – Komponenten, Tokens und Integration',
    docsDesc: 'Die vollständige NotionKit-Referenz: Live-Vorschauen, Copy-paste-Markup, Design-Tokens für beide Themes, Zustandsklassen, Mobile-Verhalten und Editor-Rezepte.',
    docsH1: 'Dokumentation',
    docsLead: 'Jede Komponente mit Live-Vorschau, dem Markup zum Kopieren, den beteiligten Klassen und ihrem Verhalten auf kleinen Schirmen. Nichts hier ist aus einem Screenshot erzeugt – die Vorschauen sind echt.',
    onThisPage: 'Auf dieser Seite',
    sections: 'Abschnitte',
    components: 'Komponenten',
    secInstall: 'Installation', secTokens: 'Design-Tokens', secTheming: 'Theming',
    secStates: 'Zustandsklassen', secContrast: 'Kontraste', secEditor: 'Editor-Integration',
    secShadow: 'Web Components', secGroups: 'Komponenten-Referenz',
    installLead: 'Ein Stylesheet. Nimm den Weg, der zu deinem Projekt passt.',
    installTheme: 'Das Theme sitzt auf <code class="nk-inline-code">&lt;html&gt;</code>. Hell ist die Vorgabe, das Attribut braucht es also nur für Dunkel:',
    installBody: 'Ergänze <code class="nk-inline-code">class="nk-body"</code> an deinem <code class="nk-inline-code">&lt;body&gt;</code>, um Schrift, Hintergrund und Textfarbe zu übernehmen. Das ist opt-in: Ohne diese Klasse fasst NotionKit nichts außerhalb seiner eigenen Klassen an.',
    tokensLead: 'Jeder visuelle Wert ist eine Custom Property. Die beiden Blöcke unten sind die gesamte Token-Fläche – <code class="nk-inline-code">:root</code> für Hell, <code class="nk-inline-code">[data-theme="dark"]</code> für die Abweichungen.',
    tokenRebrand: 'Rebranding ist eine Deklaration. Alles Abgeleitete – Fokusring, Checked-Zustände, ausgewählte Modell-Karte, Danger-Hover – wird per <code class="nk-inline-code">color-mix()</code> aus einem Token gemischt und zieht mit:',
    colToken: 'Token', colLight: 'Hell', colDark: 'Dunkel', colMeaning: 'Bedeutung',
    themingLead: 'Umgeschaltet wird über ein Attribut. Mehr bewegt sich nicht.',
    themingNote: 'Die Token-Blöcke setzen auch <code class="nk-inline-code">color-scheme</code>, damit vom Browser gezeichnete Oberflächen – Datumswähler, Zahlenfelder, Select-Popups, Scrollbars, Autofill – dem Theme ohne Zutun folgen.',
    themingPalettes: 'Rebranding live sehen: der 🎨-Button in der Kopfleiste wendet die Beispiel-Paletten aus <code class="nk-inline-code">theme-override.css</code> (Forest, Slate, Sunset, Hoher Kontrast) direkt auf diese Seite an. Die Demo-App hat denselben Schalter unter Einstellungen → Darstellung.',
    statesLead: 'NotionKit liefert Zustände, kein Verhalten. Diese Klassen setzt und entfernst du selbst; in der Library steckt kein JavaScript.',
    colClass: 'Klasse', colAppliesTo: 'Gilt für', colEffect: 'Wirkung',
    contrastLead: 'Gemessene Kontrastwerte der Standardpalette, jeweils gegen die Fläche, auf der das Paar tatsächlich sitzt. Fließtext erfüllt WCAG AA in beiden Themes deutlich. Sekundär- und Tertiärtext, die Tag-Paare, die Beschriftung des Primärbuttons und die Danger-Farbe liegen unter 4,5:1 – das sind Notions eigene Werte, bewusst beibehalten.',
    contrastNote: 'Wenn dein Projekt überall AA braucht: <code class="nk-inline-code">theme-override.css</code> enthält einen fertigen High-Contrast-Block. Einkommentieren hebt die durchgefallenen Paare an, der Rest des Designs bleibt unangetastet.',
    colPair: 'Paar', colRatio: 'Verhältnis', colVerdict: 'AA Fließtext',
    editorLead: 'NotionKit liefert keinen Editor. Was es liefert, ist <code class="nk-inline-code">.nk-block-host</code> – die optische Hülle – plus eine darin gescopte, gestylte Adapter-Schicht. Der Integrationsvertrag ist eine Klasse am Host-Element.',
    editorTip: 'TipTap / ProseMirror – der Vanilla-JS-Weg',
    editorTipBody: 'TipTap als ES-Modul von einem CDN laden und in ein Element mit <code class="nk-inline-code">nk-block-host</code> einhängen. Den Rest erledigen die Adapter-Regeln: Typografie, Überschriften, Listen, Zitate, Code, Links, Selektion und Placeholder werden aus <code class="nk-inline-code">--nk-*</code>-Tokens gestylt.',
    editorBn: 'BlockNote – der React-Weg',
    editorBnBody: 'Dieselbe Idee: die BlockNote-View in einem Block-Host rendern. <code class="nk-inline-code">.bn-container</code>, <code class="nk-inline-code">.bn-editor</code>, <code class="nk-inline-code">.bn-block-content</code>, Side-Menu und Drag-Handle sind abgedeckt, und <code class="nk-inline-code">.bn-suggestion-menu</code> übernimmt die Slash-Menü-Optik automatisch.',
    editorNovel: 'Novel – der Next.js-Weg',
    editorNovelBody: 'Novel baut auf ProseMirror auf, die TipTap-Regeln greifen also bereits. <code class="nk-inline-code">.novel-editor</code> und <code class="nk-inline-code">.novel-prose</code> bekommen Schrift und Farbe, und die Prosa-Breitenbegrenzung wird gelöst, damit die Seitenspalte sie bestimmt.',
    editorLive: 'Live: TipTap in einem Block-Host',
    editorLiveBody: 'Der Editor unten ist echtes TipTap, von einem CDN geladen, ohne eigenes CSS. Fahre über einen Block für die ＋ / ⠿-Leiste und zieh ihn, markiere Text für die Bubble-Toolbar, drücke <kbd class="nk-kbd">/</kbd> für das Slash-Menü und schalte das Theme um – alles Sichtbare kommt aus dem Adapter-Abschnitt von notionkit.css.',
    editorRecipeNote: 'Das vollständige Rezept – Slash-Menü auf <code class="nk-inline-code">@tiptap/suggestion</code>, Bubble-Toolbar, Block-Handle mit Drag &amp; Drop und Block-Menü – ist <a href="../docs-editor.js">docs-editor.js</a>, rund 300 Zeilen schlichtes JavaScript. Es setzt NotionKit-Klassen auf sein DOM (<code class="nk-inline-code">nk-slash-menu</code>, <code class="nk-inline-code">nk-bubble-menu</code>, <code class="nk-inline-code">nk-block-actions</code>, <code class="nk-inline-code">nk-menu</code>) und überlässt den Rest dem Stylesheet.',
    shadowLead: 'Das Stylesheet wird zweigeteilt ausgeliefert, damit Web Components die Komponentenregeln adoptieren können, ohne die Tokens in ihren Shadow Roots neu zu deklarieren.',
    shadowWhy: 'Warum die Teilung zählt',
    shadowWhyBody: 'Ein Shadow Root, der das ganze Sheet adoptiert, adoptiert auch die <code class="nk-inline-code">:root</code>- und <code class="nk-inline-code">[data-theme]</code>-Blöcke. In diesem Root treffen die Selektoren den eigenen Theme-Wrapper des Elements, jedes Token wird also lokal neu deklariert – und eine treffende Regel schlägt immer einen geerbten Wert. Ein <code class="nk-inline-code">:root { --nk-accent: … }</code> des Consumers käme nie an. Also nur <code class="nk-inline-code">componentsSheet</code> adoptieren und <code class="nk-inline-code">tokensCss</code> aufs Dokument legen.',
    shadowSlotted: 'Die ::slotted()-Zwillinge',
    shadowSlottedBody: 'Von außen übergebener Inhalt bleibt im Light DOM und ist kein Nachfahre von irgendetwas im Shadow Tree – <code class="nk-inline-code">.nk-callout .c-icon</code> erreicht ihn also nicht. Jede solche Regel hat deshalb einen <code class="nk-inline-code">::slotted()</code>-Zwilling daneben, 57 an der Zahl. Zwei Grenzen: <code class="nk-inline-code">::slotted()</code> trifft nur den zugewiesenen Knoten, nie etwas darin (Icons direkt übergeben, nie verpackt), und bei geslottetem Inhalt gewinnt der äußere Baum gegen den Shadow Tree.',
    shadowPoc: 'Ein lauffähiger Nachweis liegt als <a href="../elements-poc.html">elements-poc.html</a> bei – ein <code class="nk-inline-code">&lt;nk-callout&gt;</code> und ein <code class="nk-inline-code">&lt;nk-tag color="…"&gt;</code>, die <code class="nk-inline-code">componentsSheet</code> adoptieren, über <code class="nk-inline-code">data-theme</code> umthemen und das Branding der Seite übernehmen. Die Seite misst das live (sie braucht einen Build und einen Server: <code class="nk-inline-code">npm run build</code>, dann den Ordner ausliefern).',
    mobileLabel: 'Auf kleinem Schirm',
    classesLabel: 'Klassen',
    prdLabel: 'Gruppe',
    backToTop: '↑ Nach oben',
  },
};
